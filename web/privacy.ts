import { initShell } from "./common";

// Policy content is static and readable without JavaScript. This only mounts
// the same language and appearance controls used throughout the scheduler.
initShell();
